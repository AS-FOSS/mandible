# Generated by `xtask audit fixtures` (corpus/README.md's workflow) —
# reviewed under seed 20260811, verdict "correct". See that file's own
# review-any-fixture-for-machine-specific-content note before committing.

[tool]
name = "du"
version = "audit-seed20260811"
captured_with = "xtask audit"

[[capture]]
argv = ["du", "--help"]
stdout = "help.txt"

[contract]
expected_framework = "GNU argp/getopt_long"
min_status = "ok"
min_subcommands = 0
must_contain_flags = ["--null", "--all", "--apparent-size", "--block-size", "--bytes"]

