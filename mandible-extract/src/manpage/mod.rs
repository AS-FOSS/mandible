//! Tier D: man page structural extraction. Deferred entirely, behind the
//! `manpage` feature flag, off by default. Spec §7 Tier D, §14, §15.
//!
//! Module intentionally empty; `manpage` feature and `cc`/`bindgen`
//! optional deps are wired in `Cargo.toml` for a future implementation.
