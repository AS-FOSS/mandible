# Generated by `xtask audit fixtures` (corpus/README.md's workflow) —
# reviewed under seed 2, verdict "incomplete". See that file's own
# review-any-fixture-for-machine-specific-content note before committing.

[tool]
name = "filegone-bpfcc"
version = "audit-seed2"
captured_with = "xtask audit"

[[capture]]
argv = ["filegone-bpfcc", "--help"]
stdout = "help.txt"

[contract]
expected_framework = "argparse"
# "-p PID, --pid PID  trace this PID only" — verified this becomes
# short='p', value_name='PID,' (trailing comma bled in from the raw text)
# with no `long` set at all; "--pid" is dropped entirely.
must_contain_flags = ["--pid"]

[xfail]
broken = true
reason = "detected -p, but not optional --pid"
