# Generated by `xtask audit fixtures` (corpus/README.md's workflow) —
# reviewed under seed 2, verdict "incomplete". See that file's own
# review-any-fixture-for-machine-specific-content note before committing.

[bless]
provenance = "agent"

[tool]
name = "filegone-bpfcc"
version = "audit-seed2"
captured_with = "xtask audit"

[[capture]]
argv = ["filegone-bpfcc", "--help"]
stdout = "help.txt"

[contract]
expected_framework = "argparse"
# "-p PID, --pid PID  trace this PID only" — the dropped-alias defect:
# this used to become short='p', value_name='PID,' (the alias separator
# bled into the placeholder) with no `long` set at all, and "--pid" was
# dropped entirely. `grammar::alias_continues` now resumes the alias run
# past the value spec, so the row yields short='p', long='pid',
# value_name='PID'. Promoted out of [xfail].
must_contain_flags = ["--pid"]
