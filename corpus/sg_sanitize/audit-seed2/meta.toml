# Generated by `xtask audit fixtures` (corpus/README.md's workflow) —
# reviewed under seed 2, verdict "incomplete". See that file's own
# review-any-fixture-for-machine-specific-content note before committing.

[tool]
name = "sg_sanitize"
version = "audit-seed2"
captured_with = "xtask audit"

[[capture]]
argv = ["sg_sanitize", "--help"]
stdout = "help.txt"
stderr = "help.stderr.txt"

[contract]
expected_framework = "generic"
# Confirms the reviewer's own suspicion: "--count=OC|-c OC" becomes
# long='count', value_name='OC|-c' (the pipe and short alias leak
# literally into the value placeholder) with no `short` field set at all
# — "-c" is not recognized as its own flag identity.
must_contain_flags = ["-c"]

[xfail]
broken = true
reason = "close enough, verify if this '--count OC|-c OC' getting parsed into this is correct: '--count OC|c'"
