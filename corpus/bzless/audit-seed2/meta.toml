# Generated by `xtask audit fixtures` (corpus/README.md's workflow) —
# reviewed under seed 2, verdict "wrong". See that file's own
# review-any-fixture-for-machine-specific-content note before committing.

[tool]
name = "bzless"
version = "audit-seed2"
captured_with = "xtask audit"

[[capture]]
argv = ["bzless", "--help"]
stdout = "help.txt"
stderr = "help.stderr.txt"

[contract]
expected_framework = "generic"
# stdout is only a decorator banner ("------> --help <------"); the real
# flag table is on stderr (bzip2's own --help, which bzless delegates
# to). Verified the current tree parses the banner itself as ONE garbage
# flag (long='----', value_name='>') and none of the real flags on
# stderr — no flag has long=="help" at all right now.
must_contain_flags = ["--help"]

[xfail]
broken = true
reason = "it just is wrong, nothing was parsed and nothing shows, it only displays weird header decorator"
