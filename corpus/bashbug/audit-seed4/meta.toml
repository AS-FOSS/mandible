# Generated by `xtask audit fixtures` (corpus/README.md's workflow) —
# reviewed under seed 4, verdict "incomplete". See that file's own
# review-any-fixture-for-machine-specific-content note before committing.

# An agent generated this fixture; only a human may change the value
# below (corpus/README.md's `[bless]` section).
[bless]
provenance = "agent"

[tool]
name = "bashbug"
version = "audit-seed4"
captured_with = "xtask audit"

[[capture]]
argv = ["bashbug", "--help"]
stdout = "help.txt"

[contract]
expected_framework = "generic"
# The reviewer's note calls `bug-report-email-address` a flag; the usage
# line spells it `[bug-report-email-address]`, i.e. an optional *operand*,
# not an option — so the falsifiable form of "it is missing" is a
# positional, and asserting a flag by that name would pin behavior that
# would itself be wrong. The tree currently carries no positionals at all
# (its only synopsis-sourced entries are --help and --version), so this
# fails today and passes once the operand is extracted.
must_contain_positionals = ["bug-report-email-address"]

# Fleet count: the unparsed-tail-operand detector fires on 194 tools
# (194 flags) across a full-PATH sweep of 2318 tools, 2026-09-02. Calibrated
# and passing: 3 of 3 labelled members detected, 0 false alarms on seed 4.
