# Generated by `xtask audit fixtures` (corpus/README.md's workflow) —
# reviewed under seed 2, verdict "incomplete". See that file's own
# review-any-fixture-for-machine-specific-content note before committing.

[tool]
name = "eqn"
version = "audit-seed2"
captured_with = "xtask audit"

[[capture]]
argv = ["eqn", "--help"]
stdout = "help.txt"

[contract]
expected_framework = "generic"
# `usage: /usr/bin/eqn {-v | --version}` — verified the current tree has
# exactly one flag from this line: long='version' with a spurious
# value_name of literally '}' (the closing brace leaked in as if it were
# a value placeholder). '-v' is dropped entirely — no flag has short=='v'.
must_contain_flags = ["-v"]

[xfail]
broken = true
reason = "it shows almost everything correctly, but there is an optional -v and --version, but it only displays --version"
