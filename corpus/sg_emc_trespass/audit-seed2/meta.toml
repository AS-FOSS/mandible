# Generated by `xtask audit fixtures` (corpus/README.md's workflow) —
# reviewed under seed 2, verdict "wrong". See that file's own
# review-any-fixture-for-machine-specific-content note before committing.

[bless]
provenance = "agent"

[tool]
name = "sg_emc_trespass"
version = "audit-seed2"
captured_with = "xtask audit"

[[capture]]
argv = ["sg_emc_trespass", "--help"]
stdout = "help.txt"
stderr = "help.stderr.txt"
exit_code = 1

[contract]
expected_framework = "generic"
# "-hr: Set Honor Reservation bit" — verified this single-dash 2-char
# option becomes short='h', value_name='r:' (long is None). No flag
# currently has long=="hr".
must_contain_flags = ["hr"]

[xfail]
broken = true
reason = "some flags have no description, some got split because of that single dash not being able to take more than 1 character; and the usage block swallows the two prose lines under it, so `LUN`, `SP` and `EMC` are recorded as operands beside the real `DEVICE` — found by the existence oracle's positional check, which reports the first two (`EMC` escapes it by also being a line's first word)"
