# Generated by `xtask audit fixtures` (corpus/README.md's workflow) —
# reviewed under seed 2, verdict "wrong". Promoted (`[xfail]` removed)
# after the colon-separator and prose-swallowed-synopsis fix landed:
# `mandible-extract/src/help_text/sections.rs`'s `find_colon_separator_gap`
# (spec-vs-description colon rows, `-d : output debug`, `-hr: Set Honor
# Reservation bit`, `-V: print version string then exit`) and its
# `is_prose_sentence`-gated usage-block continuation check (the two
# post-synopsis sentences no longer get folded into the usage line and
# mined for fabricated `LUN`/`SP`/`EMC` operands). See that file's own
# review-any-fixture-for-machine-specific-content note before committing.

[bless]
provenance = "agent"

[tool]
name = "sg_emc_trespass"
version = "audit-seed2"
captured_with = "xtask audit"

[[capture]]
argv = ["sg_emc_trespass", "--help"]
stdout = "help.txt"
stderr = "help.stderr.txt"
exit_code = 1

[contract]
expected_framework = "generic"
min_status = "ok"
# All four real flags recovered as clean booleans, each with its own real
# description — no colon-shaped fabricated value anywhere.
must_contain_flags = ["-d", "-h", "-s", "-V"]
# The one real positional. `LUN`, `SP` and `EMC` — bare uppercase words
# mined out of the two prose sentences under the synopsis before this fix
# joined them into it — are absent; there is no `must_not_contain_positionals`
# contract field (only flags get a negative-claim field today, see
# corpus/README.md's "Stating that a flag does not exist"), so their
# absence is pinned by `expected.snap` itself: reintroducing any of the
# three changes the frozen tree and fails this fixture.
must_contain_positionals = ["DEVICE"]
# `-hr` is a genuine two-character switch `Flag::short` cannot represent
# (it only ever holds one character); the swallowed tail is `"r"`, one
# character, below `repair_single_dash_long_options`'s own
# `MIN_SWALLOWED_NAME_CHARS` floor (2) — the same deliberate ambiguity
# that leaves `-Ss`/`-ac`/`-it` unmerged elsewhere in this codebase, so it
# is not special-cased here either. It comes out as `short='h'`,
# `value_name=Some("r")` (clean, no more trailing `:`) — see
# `expected.snap`. The fleet existence oracle still attests it: `-h`'s own
# fallback reconstruction (`xtask::existence::short_candidates`) checks
# `-hr` at a word boundary, which occurs literally in `help.stderr.txt`.
