# Generated by `xtask audit fixtures` (corpus/README.md's workflow) —
# reviewed under seed 2, verdict "incomplete". See that file's own
# review-any-fixture-for-machine-specific-content note before committing.

[tool]
name = "killsnoop.bt"
version = "audit-seed2"
captured_with = "xtask audit"

[[capture]]
argv = ["killsnoop.bt", "--help"]
stdout = "help.txt"
stderr = "help.stderr.txt"

[contract]
expected_framework = "generic"
# "-vv" / "-dd" (repeated-letter bpftrace troubleshooting flags) become
# short='v'/value_name='v' and short='d'/value_name='d' — the second
# letter read as a spurious required value instead of part of the flag's
# own spelling (long stays None). No flag currently has long=="vv" or
# long=="dd" (bare specs below match `Flag::long` exactly, unlike a
# "-vv"-style spec which would only check the first character and
# spuriously match the real, separate "-v"/"-d" flags).
must_contain_flags = ["vv", "dd"]

[xfail]
broken = true
reason = "flags are missing. for example -vv and -dd missing from 'TROUBLESHOOTING OPTIONS'"
