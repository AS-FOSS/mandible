# Generated by `xtask audit fixtures` (corpus/README.md's workflow) —
# reviewed under seed 4, verdict "incomplete". The maintainer then checked
# this build by hand and reported six more defects, quoted below beside the
# machine classification of each.

# An agent generated this fixture; only a human may change the value
# below (corpus/README.md's `[bless]` section).
[bless]
provenance = "agent"

[tool]
name = "vim.basic"
version = "audit-seed4"
captured_with = "xtask audit"

[[capture]]
argv = ["vim.basic", "--help"]
stdout = "help.txt"

[contract]
expected_framework = "generic"

# The operand the tail-operand repair recovered. `Usage: vim [arguments]
# [file ..]` documents one repeated operand and the tree carried none.
# Passing; the `...` suffix holds the repetition marker too.
must_contain_positionals = ["file..."]

# Maintainer: "a flag with just '+' and '+<num>'" is not parsed.
# Maintainer: "'--' ... its not parsed".
# Machine classification: plus-prefixed option, and end-of-options marker
# dropped. Both rows sit in the same `Arguments:` block as 43 rows that do
# parse. `+<lnum>` is not asserted by spelling: `Spelling::name` carries no
# brackets, so a correct parse spells it `+` and puts `lnum` in the value,
# which the `must_value_name` entry below asserts instead.
must_contain_flags = ["+", "--", "--help"]

# Maintainer: "'-V[N][fname]' got parsed into just '-V [N]' which lost
# fname". Machine classification: second optional value dropped. The tree
# carries `-V` with value name `N`.
#
# Maintainer: "'-r (with file name)' got parsed into '-r (with'". Machine
# classification: parenthetical qualifier read as value (atlas S-098).
# Fixed: the second `-r` now carries the value name `file name`.
#
# Maintainer: "even worse for '-q' where it did not even get a
# placeholder". Machine classification: usage-alternative operand dropped.
# `or: vim [arguments] -q [errorfile]` gives `-q` with no value at all.
#
# The `+` entry is the second half of the plus-prefixed row above.
[contract.must_value_name]
"-V" = "fname"
"-r" = "file name"
"-q" = "errorfile"
"+" = "lnum"

# Maintainer: "the flag '-t tag' got parsed from usage line finely,
# however, it did not carry on the description of that flag." Machine
# classification: usage-alternative description dropped. `-t` and `-q` both
# reach the tree from `help-text-synopsis` with no description, while the
# usage line states one for each.
[contract.must_describe]
"-t" = "edit file where tag is defined"
"-q" = "edit file with first error"

[xfail]
broken = true
reason = "four defects remain. A `+`-led option row (`+`, `+<lnum>`) still reaches nothing — a fix was attempted and reverted: it fabricated flags on git-lfs (an AsciiDoc list-continuation marker in prose) and date (a row of a `%`-conversion-modifier table), because a bare `+` line carries no structural signal telling an option row apart from either. `-V[N][fname]` keeps only the first bracketed value and loses `fname`. `-h  or  --help` is read as one flag `-h` whose description begins `or --help`, so the `--help` spelling never becomes an alias. The usage alternatives `-t tag` and `-q [errorfile]` reach the tree without their own descriptions, and `-q` without its operand. Fixed: the end-of-options marker `--` (`mandible-extract/src/help_text/grammar.rs`'s `try_bare_sigil`) and the parenthetical qualifier on `-r` (`sections/emit.rs`'s `repair_parenthetical_value`), both generic-parser repairs, 2026-09-03."

# Fleet count: the unparsed-tail-operand detector fires on 150 tools
# (150 flags) across a full-PATH sweep of 2318 tools, 2026-09-03, down from
# 194 before the tail-operand repair. Calibrated and passing: 3 of 3
# labelled members detected, 0 false alarms on seed 4.
#
# end-of-options-marker: FIXED and REPAIRED, 2026-09-03
# (`xtask/src/end_of_options_marker.rs`; pre-fix fleet count 26 tools/26
# findings; post-fix full-PATH sweep 0/0, ratchet-gated at zero).
#
# The other six labelled families, after amending this entry's own
# `families` field to add the six the maintainer's review here evidences
# (`families_derived = true`, argued per quote in
# `audit/submissions/sadigaxund/4.toml`) and recalibrating against seed 4:
#   plus-prefixed-option: CALIBRATED (PASSES, 0 false alarms), fleet 13
#     tools/23 findings, clears the 5-tool bar. NOT FIXED this round — see
#     reason above; a fix needs positive evidence a `+` row sits in a real
#     option table, which no version tried so far provides safely.
#   usage-only-value-name: CALIBRATED (PASSES), fleet 76 tools/255
#     findings (`-q`'s missing operand only, not `-t`'s missing
#     description). Clears the bar. Not fixed this round.
#   second-optional-value-dropped: CALIBRATED (PASSES), fleet 13
#     tools/13 findings. Clears the bar. Not fixed this round.
#   parenthetical-qualifier-as-value: FIXED and REPAIRED. Pre-fix fleet
#     count 9 tools/9 findings; post-fix full-PATH sweep 0/0,
#     ratchet-gated at zero.
#   or-joined-alias: CALIBRATED (PASSES), fleet 30 tools/134 findings.
#     Clears the bar. Not fixed this round.
#   single-space-description-column: NOT calibratable against seed 4 —
#     jinfo, its only motivating tool, has no audit verdict in any seed.
#     Fleet 9 tools/27 findings, clears the bar on the count alone. Not
#     fixed this round.
# "Not fixed this round" is a session scope limit, not a finding that any
# of these five is unfixable or below the bar — all five clear it and are
# calibrated.

# Not reproducible on this build: the seed-4 note "shows '--' as a
# positional arg". The tree's only positional is `file`. Recorded here
# rather than dropped, and unexplained.
