# Generated by `xtask audit fixtures` (corpus/README.md's workflow) —
# reviewed under seed 4, verdict "incomplete". See that file's own
# review-any-fixture-for-machine-specific-content note before committing.

# An agent generated this fixture; only a human may change the value
# below (corpus/README.md's `[bless]` section).
[bless]
provenance = "agent"

[tool]
name = "vim.basic"
version = "audit-seed4"
captured_with = "xtask audit"

[[capture]]
argv = ["vim.basic", "--help"]
stdout = "help.txt"

[contract]
expected_framework = "generic"
# `Usage: vim [arguments] [file ..]` documents one operand, `file`, and the
# tree carries no positionals at all — the same `unparsed-positional`
# family this tool was labelled with under seed 2. Whatever the TUI is
# showing in an argument row, it is not vim's `file` operand; this asserts
# the operand that should be there, since a positional the parser never
# emitted cannot be forbidden by name.
must_contain_positionals = ["file"]

# Fleet count: the unparsed-tail-operand detector fires on 194 tools
# (194 flags) across a full-PATH sweep of 2318 tools, 2026-09-02. Calibrated
# and passing: 3 of 3 labelled members detected, 0 false alarms on seed 4.
