# Generated by `xtask audit fixtures` (corpus/README.md's workflow) —
# reviewed under seed 2, verdict "incomplete". See that file's own
# review-any-fixture-for-machine-specific-content note before committing.

[tool]
name = "naptime.bt"
version = "audit-seed2"
captured_with = "xtask audit"

[[capture]]
argv = ["naptime.bt", "--help"]
stdout = "help.txt"
stderr = "help.stderr.txt"

[contract]
expected_framework = "generic"
# Same bpftrace-delegated help text as killsnoop.bt (byte-identical
# TROUBLESHOOTING OPTIONS block): "-vv"/"-dd" become short='v'/value_name='v'
# and short='d'/value_name='d', long left None. No flag currently has
# long=="vv" or long=="dd".
must_contain_flags = ["vv", "dd"]

[xfail]
broken = true
reason = "similar issue to a command before where '-vv' and '-dd' flags are missing"
