# Generated by `xtask audit fixtures` (corpus/README.md's workflow) —
# reviewed under seed 2, verdict "wrong". See that file's own
# review-any-fixture-for-machine-specific-content note before committing.

[tool]
name = "xfs_io"
version = "audit-seed2"
captured_with = "xtask audit"

[[capture]]
argv = ["xfs_io", "--help"]
stdout = "help.txt"
stderr = "help.stderr.txt"
exit_code = 1

[contract]
expected_framework = "generic"
# Two defects were reported here and only one of them is this contract's.
#
# FIXED: "[-adfinrRstVx]" (a bundle of single-char flags) used to become ONE
# entry — short='a', value_name='dfinrRstVx' — and all eleven members are
# their own flag now, along with -m and -p keeping their values.
#
# STILL BROKEN, and what keeps this fixture [xfail]: "[[-c|-C] cmd]..." is a
# nested bracket group — an alternation of two flags sharing one operand —
# and neither -c nor -C reaches the tree at all. That is a different family
# from the bundle (the members are separated by '|', not glued), so the
# cluster grammar neither helps nor hinders it, and this fixture goes on
# holding the line for it.
must_contain_flags = ["-c", "-C"]

[xfail]
broken = true
reason = "the bundle half ([-adfinrRstVx]) is fixed; what remains is the nested alternation group [[-c|-C] cmd]..., whose two flags reach the tree not at all"
