# Generated by `xtask audit fixtures` (corpus/README.md's workflow) —
# reviewed under seed 2, verdict "wrong". See that file's own
# review-any-fixture-for-machine-specific-content note before committing.

[tool]
name = "xfs_io"
version = "audit-seed2"
captured_with = "xtask audit"

[[capture]]
argv = ["xfs_io", "--help"]
stdout = "help.txt"
stderr = "help.stderr.txt"
exit_code = 1

[contract]
expected_framework = "generic"
# "[-adfinrRstVx]" (a bundle of single-char flags) becomes ONE entry:
# short='a', value_name='dfinrRstVx', value_kind=Required — every letter
# after the first is swallowed as a single required value. Reviewer named
# "-c and -C" specifically (from "[[-c|-C] cmd]..."); verified neither
# exists as a flag at all right now — only a/m/p survived.
must_contain_flags = ["-c", "-C"]

[xfail]
broken = true
reason = "singe dash issue + missing -c and -C, see the original text"
