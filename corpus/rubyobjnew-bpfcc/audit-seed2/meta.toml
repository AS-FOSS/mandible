# Generated by `xtask audit fixtures` (corpus/README.md's workflow) —
# reviewed under seed 2, verdict "incomplete". See that file's own
# review-any-fixture-for-machine-specific-content note before committing.

[tool]
name = "rubyobjnew-bpfcc"
version = "audit-seed2"
captured_with = "xtask audit"

[[capture]]
argv = ["rubyobjnew-bpfcc", "--help"]
stdout = "help.txt"

[contract]
expected_framework = "argparse"
# The reviewer's note is about the dropped `pid`/`interval` positionals,
# but `CommandNode::positionals` has no [contract] field to assert on —
# see corpus/README.md, no `must_contain_positionals` exists yet. Same
# same-line-long-form-drop defect as javaflow-bpfcc's identical argparse
# style: "-l {c,java,ruby,tcl}, --language {...}" becomes short='l' only,
# garbled value_name with trailing comma. --language/--top-count/
# --top-size are all currently absent.
must_contain_flags = ["--language", "--top-count", "--top-size"]

[xfail]
broken = true
reason = "missing positinal arguments, i think we never taken those into account"
