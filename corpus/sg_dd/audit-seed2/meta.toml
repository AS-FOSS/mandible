# Generated by `xtask audit fixtures` (corpus/README.md's workflow) —
# reviewed under seed 2, verdict "wrong". See that file's own
# review-any-fixture-for-machine-specific-content note before committing.

[tool]
name = "sg_dd"
version = "audit-seed2"
captured_with = "xtask audit"

[[capture]]
argv = ["sg_dd", "--help"]
stdout = "help.txt"
stderr = "help.stderr.txt"

[contract]
expected_framework = "generic"
# The usage synopsis wraps onto a second, blank-line-separated line
# ("[blk_sgio=0|1] ... [--progress] [--verify]"). Verified only the FIRST
# line's flags (--dry-run/--help/--verbose/--version) are captured; the
# continuation line's `--progress`/`--verify` are dropped entirely, as is
# the whole "where:" section carrying every flag's real description.
must_contain_flags = ["--progress", "--verify"]

[xfail]
broken = true
reason = "same conditional dynamic flags or whatever bullshit this is"
