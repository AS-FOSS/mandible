# Generated by `xtask audit fixtures` (corpus/README.md's workflow) —
# reviewed under seed 2, verdict "wrong". See that file's own
# review-any-fixture-for-machine-specific-content note before committing.
#
# Promoted from [xfail] by the bare-block-extent repair
# (`help_text::sections::bare_block_end`): `sg_dd`'s `where:` operand table
# is a bare-word block whose own last six rows are *flag* rows at that same
# indent, so the block ran through them and all six became choices of
# nothing. `--progress` and `--verify` are documented nowhere else in this
# document and were lost outright; the other four reached the tree from the
# synopsis alone, stripped of every description.
#
# What this promotion does NOT claim to have fixed, stated so the next
# reader does not mistake a green fixture for a complete parse: the second
# synopsis paragraph (`[blk_sgio=0|1] ... [--progress] [--verify]`, after a
# blank line) is still not read as usage — `result.usage` below holds only
# the first paragraph, and the `where:` table's KEY=VALUE operands still
# have nowhere in the IR to go. The flags are recovered by the operand
# table, not by the synopsis. See spec §13.1e: the blank-line synopsis
# resumption remains a singleton with no detector and no fix.

[bless]
provenance = "human"

[tool]
name = "sg_dd"
version = "audit-seed2"
captured_with = "xtask audit"

[[capture]]
argv = ["sg_dd", "--help"]
stdout = "help.txt"
stderr = "help.stderr.txt"

[contract]
expected_framework = "generic"
# Strengthened on promotion (spec §13.2's rule: a promoted fixture asserts
# the repair's own evidence, not just the two flags that were missing).
# `--dry-run`/`--help`/`--verbose`/`--version` are here because a repair
# that over-consumed the table would satisfy `--progress`/`--verify` alone
# while destroying the four that used to survive.
must_contain_flags = [
    "--dry-run",
    "--help",
    "--progress",
    "--verbose",
    "--verify",
    "--version",
]
verdict_scope = ["flags"]
