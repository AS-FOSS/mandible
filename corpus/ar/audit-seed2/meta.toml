# Generated by `xtask audit fixtures` (corpus/README.md's workflow) —
# reviewed under seed 2, verdict "wrong". See that file's own
# review-any-fixture-for-machine-specific-content note before committing.

[tool]
name = "ar"
version = "audit-seed2"
captured_with = "xtask audit"

[[capture]]
argv = ["ar", "--help"]
stdout = "help.txt"

[contract]
expected_framework = "generic"
# " commands:" (leading-space indented, unlike a flush-left heading) lists
# 8 real operation letters (d/m/p/q/r/s/t/x). Verified the whole section —
# that heading plus every one of its 8 entries, and 2 more sections after
# it — gets swallowed wholesale into a single giant `usage` string; the
# tree has zero subcommands and zero flags for any of it. min_subcommands
# is set conservatively to 1 (not 8) since the eventual fix's exact shape
# (an indented heading may or may not count as `heading_attested`) isn't
# ours to predict — only that it must be more than the 0 it is now.
min_subcommands = 1

[xfail]
broken = true
reason = "help text shows subcommands but they are not parsed, weirdly displayed. one other command had similar format"
