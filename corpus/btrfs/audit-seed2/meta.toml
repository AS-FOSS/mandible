# Generated by `xtask audit fixtures` (corpus/README.md's workflow) —
# reviewed under seed 2, verdict "incomplete". See that file's own
# review-any-fixture-for-machine-specific-content note before committing.

[tool]
name = "btrfs"
version = "audit-seed2"
captured_with = "xtask audit"

[[capture]]
argv = ["btrfs", "--help"]
stdout = "help.txt"

[contract]
expected_framework = "generic"
# Dozens of "btrfs <group> <verb> [options] <args>\n    <description>"
# entries (e.g. "btrfs balance start [options] <path>") — verified none
# of them becomes a subcommand; the tree has zero, not `verbatim` (no
# `unparsed` either — the lines are just silently dropped).
min_subcommands = 1

[xfail]
broken = true
reason = "it lists out the subcommands as combinations of the options therefore current framework detection failed to detect subcommmands."
