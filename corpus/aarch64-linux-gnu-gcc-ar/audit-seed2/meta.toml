# Generated by `xtask audit fixtures` (corpus/README.md's workflow) —
# reviewed under seed 2, verdict "wrong". See that file's own
# review-any-fixture-for-machine-specific-content note before committing.

[tool]
name = "aarch64-linux-gnu-gcc-ar"
version = "audit-seed2"
captured_with = "xtask audit"

[[capture]]
argv = ["aarch64-linux-gnu-gcc-ar", "--help"]
stdout = "help.txt"

[contract]
expected_framework = "generic"
# Byte-identical binutils `ar` help format (same as corpus/ar's fixture,
# just the tool name substituted). Verified the whole " commands:" section
# and everything after it gets swallowed into one giant `usage` string;
# zero subcommands currently. Conservative floor, not the real count (8).
min_subcommands = 1

[xfail]
broken = true
reason = "same as similar other command"
