# Generated by `xtask audit fixtures` (corpus/README.md's workflow) —
# reviewed under seed 2, verdict "incomplete". See that file's own
# review-any-fixture-for-machine-specific-content note before committing.

[tool]
name = "javaflow-bpfcc"
version = "audit-seed2"
captured_with = "xtask audit"

[[capture]]
argv = ["javaflow-bpfcc", "--help"]
stdout = "help.txt"

[contract]
expected_framework = "argparse"
# The reviewer's note is about the dropped `pid` positional, but
# `CommandNode::positionals` has no [contract] field to assert on — see
# corpus/README.md, no `must_contain_positionals` exists yet. Verified
# there's a second, contract-testable defect in the same capture: every
# "-x VALUE, --longname VALUE" line loses its long form entirely (e.g.
# "-l {java,perl,...}, --language {...}" becomes short='l' only, with a
# garbled value_name carrying a trailing comma and the unclosed brace
# group). --language/--method/--class are all currently absent.
must_contain_flags = ["--language", "--method", "--class"]

[xfail]
broken = true
reason = "there is a positional argument 'pid' which is not parsed, but im not sure how it should be"
