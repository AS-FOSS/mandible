# Generated by `xtask audit fixtures` (corpus/README.md's workflow) —
# reviewed under seed 2, verdict "wrong". See that file's own
# review-any-fixture-for-machine-specific-content note before committing.

[tool]
name = "cache_restore"
version = "audit-seed2"
captured_with = "xtask audit"

[[capture]]
argv = ["cache_restore", "--help"]
stdout = "help.txt"

[contract]
expected_framework = "generic"
# Every option here is spelled `{-h|--help}` (curly braces, not the usual
# `[-h|--help]`) — verified this defeats flag detection completely: the
# current tree has zero flags at all, and "Options:" itself leaked into
# the root description instead. Once curly-brace option groups are
# recognized, at least the plain `--help`/`--version` flags should appear.
must_contain_flags = ["--help", "--version", "--input", "--output"]

[xfail]
broken = true
reason = "flags were not detected"
