# Generated by `xtask audit fixtures` (corpus/README.md's workflow) —
# reviewed under seed 2, verdict "wrong". See that file's own
# review-any-fixture-for-machine-specific-content note before committing.

[tool]
name = "filefrag"
version = "audit-seed2"
captured_with = "xtask audit"

[[capture]]
argv = ["filefrag", "--help"]
stdout = "help.txt"
stderr = "help.stderr.txt"
exit_code = 1

[contract]
expected_framework = "generic"
# "[-BeEksvxX]" (a bundle of single-char flags) becomes ONE entry:
# short='B', value_name='eEksvxX', value_kind=Required — every letter
# after the first is swallowed as a single required value. No flag
# currently has short=='v' (the only other flag is 'b', lowercase,
# distinct from this bundle).
must_contain_flags = ["-v"]

[xfail]
broken = true
reason = "possibly wrong flags"
