# Generated by `xtask audit fixtures` (corpus/README.md's workflow) —
# reviewed under seed 2, verdict "wrong". See that file's own
# review-any-fixture-for-machine-specific-content note before committing.

[tool]
name = "gethostlatency-bpfcc"
version = "audit-seed2"
captured_with = "xtask audit"

[[capture]]
argv = ["gethostlatency-bpfcc", "--help"]
stdout = "help.txt"

[contract]
expected_framework = "argparse"
# Same "-p PID, --pid PID" line as filegone-bpfcc: verified this becomes
# short='p', value_name='PID,' with no `long` set; "--pid" is dropped
# entirely.
must_contain_flags = ["--pid"]

[xfail]
broken = true
reason = "similar -p and --pid issue"
