# Generated by `xtask audit fixtures` (corpus/README.md's workflow) —
# reviewed under seed 2, verdict "wrong". See that file's own
# review-any-fixture-for-machine-specific-content note before committing.

[tool]
name = "opensnoop.bt"
version = "audit-seed2"
captured_with = "xtask audit"

[[capture]]
argv = ["opensnoop.bt", "--help"]
stdout = "help.txt"
stderr = "help.stderr.txt"

[contract]
expected_framework = "generic"
# Same bpftrace-delegated help text as killsnoop.bt: "-kk" becomes
# short='k', value_name='k', long left None. No flag currently has
# long=="kk".
must_contain_flags = ["kk"]

[xfail]
broken = true
reason = "similar issue to '-vv' and '-dd', but this time it happened to '-kk'"
