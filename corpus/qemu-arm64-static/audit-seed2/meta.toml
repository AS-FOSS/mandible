# Generated by `xtask audit fixtures` (corpus/README.md's workflow) —
# reviewed under seed 2, verdict "wrong". See that file's own
# review-any-fixture-for-machine-specific-content note before committing.

[tool]
name = "qemu-arm64-static"
version = "audit-seed2"
captured_with = "xtask audit"

[[capture]]
argv = ["qemu-arm64-static", "--help"]
stdout = "help.txt"

[contract]
expected_framework = "generic"
# 3-column table (Argument | Env-variable | Description). PROMOTED out of
# [xfail]: the single-dash long-option repair
# (`sections::repair_single_dash_long_options`) landed and this document is
# the family's clearest specimen. The "-help" row (a distinct single-dash
# multi-char option, immediately after "-h") used to become a SECOND entry
# with short='h', value_name='elp' — "-help" read as "-h" taking a required
# value literally named "elp". Eleven of this tool's rows went that way;
# all eleven now carry their real name (`-help`, `-cpu`, `-dfilter`,
# `-one-insn-per-tb`, `-singlestep`, `-strace`, `-seed`, `-trace`,
# `-version`, `-perfmap`, `-jitdump`).
#
# The rows the repair must NOT touch are the point of keeping this fixture:
# `-g port`, `-L path`, `-s size`, `-B address`, `-R size`, `-D logfile`,
# `-p pagesize`, `-r uname`, `-0 argv0` are genuine value-taking short
# flags on adjacent rows of the same table, separated from the long options
# by nothing but a space, and `expected.snap` freezes every one of them
# unchanged.
must_contain_flags = ["help", "cpu", "one-insn-per-tb"]
