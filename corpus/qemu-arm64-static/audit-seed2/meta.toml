# Generated by `xtask audit fixtures` (corpus/README.md's workflow) —
# reviewed under seed 2, verdict "wrong". See that file's own
# review-any-fixture-for-machine-specific-content note before committing.

[tool]
name = "qemu-arm64-static"
version = "audit-seed2"
captured_with = "xtask audit"

[[capture]]
argv = ["qemu-arm64-static", "--help"]
stdout = "help.txt"

[contract]
expected_framework = "generic"
# 3-column table (Argument | Env-variable | Description). Verified: the
# "-help" row (a distinct single-dash multi-char option, immediately after
# "-h") becomes a SECOND entry with short='h', value_name='elp' — i.e.
# "-help" reads as "-h" taking a required value literally named "elp".
# No flag currently has long=="help".
must_contain_flags = ["help"]

[xfail]
broken = true
reason = "new 3 column format, also badly parsed flags, it separated the -help into '-h elp'"
