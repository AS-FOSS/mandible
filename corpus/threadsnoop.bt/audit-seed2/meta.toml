# Generated by `xtask audit fixtures` (corpus/README.md's workflow) —
# reviewed under seed 2, verdict "wrong". See that file's own
# review-any-fixture-for-machine-specific-content note before committing.

[tool]
name = "threadsnoop.bt"
version = "audit-seed2"
captured_with = "xtask audit"

[[capture]]
argv = ["threadsnoop.bt", "--help"]
stdout = "help.txt"
stderr = "help.stderr.txt"

[contract]
expected_framework = "generic"
# Same bpftrace-delegated help text as killsnoop.bt: "-vv" becomes
# short='v', value_name='v', long left None. No flag currently has
# long=="vv".
must_contain_flags = ["vv"]

[xfail]
broken = true
reason = "'-vv' issue"
